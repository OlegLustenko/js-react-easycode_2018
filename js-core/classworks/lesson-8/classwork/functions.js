function test(fn) {
  let counter = 0;
  return doubleNumber;
}

var doubleNumber = function(number) {
  console.log(counter);
  console.log(number * 2);
};

function randomArguments() {
  console.log(arguments);
  return arr.reduce(function(sum, number) {
    return sum + number;
  }, 0);

  // console.log(a, b, c, g);
}

console.log(randomArguments(10));

var someObj = {
  sum: function(number1, number2) {
    return number1 + number2;
  }
};

const pseudoArray = {
  0: function() {},
  1: 'world',
  2: 'studentnts',
  3: 'qwe',
  length: 4
};
